// Models
const Folder = require('../../models/folder.model');
const File = require('../../models/file.model');

/**
 * @function createFolders
 * @description Create folders from a file server to MongoDB.
 * @param { object } shelf 
 */
const createFolders = (shelf) => {
    return true;
};

module.exports = {
    createFolders
};